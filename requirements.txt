# Pinned to exact versions so a rebuild cannot silently change behaviour.
# Upgrade deliberately: run the differential test and the route smoke test,
# and check that url_for still resolves every template route name.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
authlib==1.8.0
httpx2==2.12.0
python-multipart==0.0.32
jinja2==3.1.6
click==8.5.0
orjson==3.12.0
python-dotenv==1.2.3
tqdm==4.70.0
regex==2026.9.10
posthog==7.49.0
itsdangerous==2.2.0
pyyaml==6.0.3
boto3==1.43.91
psycopg[binary]==3.3.5
psycopg-pool==3.3.1
