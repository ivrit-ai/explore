# Pinned deliberately. The app's templates resolve routes by the name= kwarg on
# the route decorators, and Starlette 1.x drops those names, so the whole web
# stack is held at the versions this app is known to run on. Bump on purpose,
# with a check that url_for still resolves, not implicitly at build time.
fastapi==0.133.1
starlette==0.52.1
uvicorn[standard]==0.41.0
authlib==1.6.8
httpx==0.28.1
python-multipart==0.0.22
jinja2==3.1.6
click==8.3.1
orjson==3.11.7
python-dotenv==1.2.1
tqdm==4.67.3
regex==2026.2.19
posthog==7.9.4
itsdangerous==2.2.0
pyyaml==6.0.3
boto3==1.43.91
psycopg[binary]==3.3.5
psycopg-pool==3.3.1
